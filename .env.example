SECRET_KEY=""
JWT_SIGNING_KEY=""
DJANGO_SETTINGS_MODULE=backend.settings.production
DEBUG=False
ALLOWED_HOSTS="localhost,backend,yourdomain.com"
CORS_ALLOWED_ORIGINS="https://yourdomain.com"

# ---------------------------------------------------------------------------
# Database — choose ONE of the two options below.
# ---------------------------------------------------------------------------

# Option A: connection URL (recommended)
# Scheme determines the engine:
#   PostgreSQL  ->  postgresql://user:password@host:5432/dbname
#   MySQL       ->  mysql://user:password@host:3306/dbname
#
# DATABASE_URL=""

# Option B: individual PostgreSQL variables (legacy / Docker Compose default)
POSTGRES_DB=""
POSTGRES_USER=""
POSTGRES_PASSWORD=""
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ---------------------------------------------------------------------------
# Media file storage — set to "local" (default), "s3", or "azure"
# ---------------------------------------------------------------------------
MEDIA_STORAGE=local

# S3 (requires: pip install django-storages[s3])
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_REGION_NAME=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Azure Blob Storage (requires: pip install django-storages[azure])
# AZURE_ACCOUNT_NAME=
# AZURE_ACCOUNT_KEY=
# AZURE_CONTAINER=
